\*\*PROJECT ARTHA — Decision Log\*\*	v5  |  April 2026  |  Confidential



\*\*PROJECT ARTHA\*\*



Decision Log  |  Architecture \& Financial Logic Decisions  |  v5  |  April 2026



This document tracks BUILD decisions — “why did we choose Supabase?”, “why proper compounding?”. NOT the in-app investment decisions audit log.



\*\*v5 CHANGES: +9 new decisions (DEC-023 through DEC-031). New Security category added. DEC-001 superseded by DEC-023 (GitHub Pages). DEC-010 superseded by DEC-024 (Claude Code).\*\*



\# Decision Log



Entries are chronological. New decisions appended at the bottom. Decisions added in v5 are marked NEW. Status: Implemented, Pending, Reversed, or Superseded.



| \*\*#\*\* | \*\*Date\*\* | \*\*Category\*\* | \*\*Decision\*\* | \*\*Context\*\* | \*\*Options Considered\*\* | \*\*Rationale\*\* | \*\*Status\*\* |

| --- | --- | --- | --- | --- | --- | --- | --- |

| \*\*DEC-001\*\* | Mar 26 | \*\*Architecture\*\* | \*\*Netlify over Vercel for hosting\*\* | Needed free-tier static site hosting. | Vercel (setup issues), Netlify (worked), GitHub Pages. | Netlify worked immediately. Free tier: 300 build min/month. | \*\*Superseded by DEC-023\*\* |

| \*\*DEC-002\*\* | Mar 26 | \*\*Architecture\*\* | \*\*Resend over Gmail SMTP for email\*\* | Gmail App Password auth was unreliable. | Gmail SMTP, Resend API, SendGrid. | Single HTTP call, reliable, free 3,000 emails/month. | \*\*Implemented\*\* |

| \*\*DEC-003\*\* | Mar 26 | \*\*Architecture\*\* | \*\*GitHub Actions for scheduled alerts\*\* | Need daily automated email at 6:30pm IST. | GitHub Actions, AWS Lambda, Vercel cron. | Zero cost, no server, 2,000 free min/month. | \*\*Implemented\*\* |

| \*\*DEC-004\*\* | Mar 26 | \*\*Data Model\*\* | \*\*localStorage for config persistence\*\* | Single-user tool needs session persistence. | localStorage, Supabase, JSON file. | Appropriate for single-user. No backend until signal history needed. | \*\*Implemented\*\* |

| \*\*DEC-005\*\* | Mar 26 | \*\*Provider\*\* | \*\*mfapi.in for NAV data\*\* | Need reliable free MF NAV data. | mfapi.in, AMFI direct CSV, paid APIs. | Free, public, no auth, reliable. One daily NAV. | \*\*Implemented\*\* |

| \*\*DEC-006\*\* | Mar 26 | \*\*Architecture\*\* | \*\*React + Vite over plain HTML\*\* | App growing beyond simple HTML. | Plain HTML, React + Vite, Next.js. | Component reuse. Recharts for charts. Vite for fast builds. | \*\*Implemented\*\* |

| \*\*DEC-007\*\* | Apr 26 | \*\*Signal Logic\*\* | \*\*Rolling avg deviation as primary signal\*\* | Need a signal to identify meaningful NAV dips. | Rolling avg, Bollinger Bands, Fixed threshold. | Tunable via period and threshold. Easy to validate. | \*\*Implemented\*\* |

| \*\*DEC-008\*\* | Apr 26 | \*\*Financial Rule\*\* | \*\*Goal-aware advice tiers based on horizon\*\* | Same signal should produce different advice per goal horizon. | Uniform advice, Time-based tiers, Dynamic risk. | Five tiers from Imminent (<2Y) to Long (>15Y). | \*\*Implemented\*\* |

| \*\*DEC-009\*\* | Apr 26 | \*\*Architecture\*\* | \*\*Supabase (Postgres) as target database\*\* | Need relational DB for goals, signal history, audit log. | Supabase, Firebase, PlanetScale. | Open-source (no lock-in), free 500MB, React SDK, built-in auth. | \*\*Pending\*\* |

| \*\*DEC-010\*\* | Apr 26 | \*\*Architecture\*\* | \*\*Superseded: Codespaces as primary dev env\*\* | Was evaluating Codespaces vs local Node.js. | Local Node.js, Codespaces, Hybrid. | Originally chose Codespaces. Superseded by DEC-024. | \*\*Superseded by DEC-024\*\* |

| \*\*DEC-011\*\* | Apr 26 | \*\*Data Model\*\* | \*\*Decisions audit log for investment tracking\*\* | No way to validate if signal rules produce better outcomes. | No tracking, manual spreadsheet, in-app audit log. | Auto-fills 30d/90d NAV. Computes outcome return. Essential for system validation. | \*\*Pending\*\* |

| \*\*DEC-012\*\* | Apr 26 | \*\*Financial Rule\*\* | \*\*7 goal categories with type-specific rules\*\* | Different goals need different CAGR, horizon, equity cutoff. | Single generic goal type vs 7 distinct types. | Each has default CAGR, horizon, equity cutoff, non-negotiable flag. | \*\*Implemented\*\* |

| \*\*DEC-013\*\* | Apr 26 | \*\*Financial Rule\*\* | \*\*Off-track engine: 5 levers with type-priority\*\* | Need corrective action suggestions when goals are off-track. | Generic levers vs type-specific priority order. | Priority order varies by goal type. Deterministic logic. | \*\*Implemented\*\* |

| \*\*DEC-014\*\* | Apr 26 | \*\*UX\*\* | \*\*Required CAGR shown only when amber/red\*\* | Showing required CAGR for healthy goals adds noise. | Always show vs show only when actionable. | Reduces noise for healthy goals. Surfaces when actionable. | \*\*Implemented\*\* |

| \*\*DEC-015\*\* | Apr 26 | \*\*Data Model\*\* | \*\*Refined goal schema for v4 localStorage\*\* | Need richer schema for corpus tracking and goal types. | Extend v3 schema vs new v4 schema with migration. | yearsLeft computed not stored. goalType added. Migration function. | \*\*Implemented\*\* |

| \*\*DEC-016\*\* | Apr 26 | \*\*Data Model\*\* | \*\*CAS upload separate from Goal Compass\*\* | CAS parsing is different from goal stress-testing. | Combined agent vs separate backlog items. | Different doc type, parsing logic, purpose. AG-3A created. | \*\*Pending\*\* |

| \*\*DEC-017\*\* | Apr 26 | \*\*Financial Rule\*\* | \*\*Allocation targets deferred to AG-1/AG-4\*\* | Fund-to-goal mapping suffices for now. | Add allocation targets now vs defer. | Not needed until Portfolio Pulse and Fund Finder. | \*\*Pending\*\* |

| \*\*DEC-018\*\* | Apr 26 | \*\*Financial Rule\*\* | \*\*RDs/FDs in corpus as lump value\*\* | How to account for fixed income instruments in goal corpus. | Per-instrument tracking vs lump value. | Predictable returns. Lumping is sound for projections. AE-4 for breakdown. | \*\*Pending\*\* |

| \*\*DEC-019\*\* | Apr 26 | \*\*Financial Rule\*\* | \*\*Proper compounding for SIP projections\*\* | Most Indian MF calculators use simplified annual/12. | (1+annual)^(1/12)-1 vs annual/12. | \~8% more conservative over 20Y. Deliberate for financial planning. | \*\*Implemented\*\* |

| \*\*DEC-020\*\* | Apr 26 | \*\*Data Model\*\* | \*\*Soft delete (abandon) for goals\*\* | Need to remove goals without breaking data. | Hard delete vs soft delete via status. | Hard delete deferred to Supabase. Zeroing out is poor workaround. | \*\*Pending (SW-9)\*\* |

| \*\*DEC-021\*\* | Apr 26 | \*\*Architecture\*\* | \*\*Bridge architecture for goal health dashboard\*\* | Need v4 features without breaking v3 signal system. | Full rewrite vs bridge layer. | GoalDashboard converts legacy format on-the-fly. Full v4-native deferred to AR-1. | \*\*Implemented\*\* |

| \*\*DEC-022\*\* | Apr 26 | \*\*Signal Logic\*\* | \*\*Neutral signals check horizon for derisking\*\* | Neutral signals showed “no action” even for imminent goals. | Keep neutral as-is vs add horizon check. | Material financial safety improvement. Goals within equity cutoff get derisk warning. | \*\*Implemented\*\* |

| \*\*DEC-023\*\* | Apr 26 | \*\*Architecture\*\* | \*\*NEW: GitHub Pages over Netlify for hosting\*\* | Netlify burning 50% of 300 build credits in first week of active dev. Unsustainable as features increase. | Continue Netlify (risk hitting limit), Cloudflare Pages (500 builds/mo), GitHub Pages (unlimited, free). | GitHub Pages: unlimited builds, free, auto-deploy via GitHub Actions (already used for alerts). No preview URLs for branches, but local dev server (npm run dev) covers this for solo developer. | \*\*Pending\*\* |

| \*\*DEC-024\*\* | Apr 26 | \*\*Architecture\*\* | \*\*NEW: Claude Code + local Node.js for development\*\* | Chat-based copy-paste workflow was slow (hours per feature). Peers using CLI agents report 3-5x productivity. | Continue Claude.ai chat, Gemini CLI (free), Claude Code (Enterprise). | Claude Code via Enterprise plan: zero personal cost, best code quality, reads entire codebase. Local Node.js eliminates Codespaces hour limits. CLAUDE.md provides persistent project context. | \*\*Implemented\*\* |

| \*\*DEC-025\*\* | Apr 26 | \*\*Architecture\*\* | \*\*NEW: Multi-LLM cascade as P1 core requirement\*\* | App needs LLM for chat/agents. Single provider = single point of failure. Free tiers can change. | Hardcode Claude API vs build abstraction layer early. | Provider abstraction layer (AR-5) promoted to P1. Built into first LLM feature (SW-4). Cascade: primary free tier → secondary free tier → cached stale response → deterministic-only mode. Every feature must work without LLM. | \*\*Pending\*\* |

| \*\*DEC-026\*\* | Apr 26 | \*\*Security\*\* | \*\*NEW: LLM response validation layer\*\* | LLMs can produce plausible but wrong financial recommendations. Weaker free-tier models increase this risk. | Trust LLM output vs validate deterministically before display. | Every actionable recommendation sanity-checked: SIP caps, CAGR consistency, timeline bounds, risk limits. If validation fails, show deterministic calculation with override note. Log discrepancies. | \*\*Pending\*\* |

| \*\*DEC-027\*\* | Apr 26 | \*\*Security\*\* | \*\*NEW: Data minimisation for LLM prompts\*\* | Free-tier LLM APIs may use input data for training. App sends financial goals, SIPs, corpus values. | Send raw data vs anonymise/aggregate before sending. | Never send absolute rupee amounts, specific fund names, or personal identifiers. Use percentages, ratios, time horizons. Programmatic prompt templates enforce anonymisation. Applies to ALL providers. | \*\*Pending\*\* |

| \*\*DEC-028\*\* | Apr 26 | \*\*Architecture\*\* | \*\*NEW: CLAUDE.md for project context\*\* | Uploading 3 documents every Claude session is cumbersome and error-prone. | Continue manual uploads vs CLAUDE.md in repo root. | CLAUDE.md contains condensed project context. Claude Code reads it automatically. Also works as handoff documentation when Enterprise access ends. | \*\*Implemented\*\* |

| \*\*DEC-029\*\* | Apr 26 | \*\*Architecture\*\* | \*\*NEW: Build-phase vs run-phase cost separation\*\* | Was conflating “free tier” for dev tools with “free tier” for production dependencies. | Single cost constraint vs separate build/run analysis. | Build-phase: Claude Code via Enterprise (zero personal cost now, not needed post-build). Run-phase: all dependencies must have free tier or open-source alternative. App must work at ₹0/month after build is complete. | \*\*Implemented\*\* |

| \*\*DEC-030\*\* | Apr 26 | \*\*Architecture\*\* | \*\*NEW: Build own LLM orchestration, no frameworks\*\* | Need multi-agent architecture. Evaluated LangGraph, CrewAI, AutoGen, MCP servers. | LangGraph (heavyweight, frequent breaking changes), CrewAI/AutoGen (enterprise-scale), MCP server (premature without backend), Build own thin abstraction (simple, full learning). | Custom provider abstraction + agent modules. \~200-300 lines. Each agent is a JS module calling callLLM(). No framework dependency. Maximises learning. MCP server evaluated post-Supabase migration (P3). | \*\*Pending\*\* |

| \*\*DEC-031\*\* | Apr 26 | \*\*Security\*\* | \*\*NEW: LLM traceability and tool-call audit log\*\* | Need to trace what LLMs do internally: tools called, I/O, reasoning, token usage. Essential for debugging agents and comparing model quality. | No logging (flying blind), Console-only logging (temporary), Full Supabase trace table (complete audit trail). | Console-log traces during Sprint 2 (chat panel). Full Supabase llm\_traces table in Sprint 3. Captures: provider, model, prompt, response, tool calls, latency, tokens, validation pass/fail. Essential for multi-model quality comparison. | \*\*Pending\*\* |

| \*\*DEC-032\*\* | Apr 24, 2026 | \*\*Architecture\*\* | \*\*Keep Codespaces as backup dev environment; Local Node.js preferred when available\*\* | Codespaces was the original dev environment (DEC-010). Claude Code integrates better with local Node.js — no hour limits, faster file access, native hot reload. | Continue Codespaces as primary, Local Node.js, Hybrid (local primary / Codespaces fallback). | Local Node.js chosen as primary. Codespaces retained as backup when local env unavailable. Claude Code running locally with npm installed. | \*\*Implemented\*\* |

| \*\*DEC-033\*\* | Apr 16, 2026 | \*\*Architecture\*\* | \*\*peaceiris/actions-gh-pages@v4 for GitHub Pages deployment\*\* | Executing DEC-023 (GitHub Pages over Netlify). Needed a deploy action for the CI pipeline. | peaceiris/actions-gh-pages@v4 (10K+ stars, most popular), JamesIves/github-pages-deploy-action, manual gh-pages push script. | Chose peaceiris — most widely used GH Pages deploy action. Triggers on push to main, runs npm ci + npm run build, deploys dist/ to gh-pages branch. Added SPA routing fix via public/404.html redirect pattern. Vite base set to /signal-watch/ for correct asset paths. | \*\*Implemented\*\* |

| \*\*DEC-034\*\* | Apr 16, 2026 | \*\*Financial Rule\*\* | \*\*Conviction scoring: 5-factor weighted model for dip prioritisation\*\* | When multiple funds show Buy Dip simultaneously, user needs guidance on where to deploy a lump sum. Need a systematic scoring model, not ad-hoc gut feel. | Single-factor ranking (dip depth only), Equal allocation across all dips, Multi-factor weighted model. | 5-factor weighted model: dip depth 30%, market P/E 20%, drawdown from 52W high 15%, goal horizon 20%, goal health 15%. Weights chosen to prioritise signal quality (dip depth) while ensuring goal context shapes the recommendation. Emergency funds excluded from equity dips entirely (score=0). Goals <2Y also excluded (capital preservation). Suggested amounts proportional to scores, rounded to ₹500. | \*\*Implemented\*\* |

| \*\*DEC-035\*\* | Apr 24, 2026 | \*\*Architecture\*\* | \*\*HashRouter over BrowserRouter for GitHub Pages\*\* | GitHub Pages serves static files and cannot handle direct URL navigation with BrowserRouter — it looks for literal folder paths (e.g., /signal-watch/chat) and returns 404. This breaks bookmarked multi-page URLs. | BrowserRouter + 404.html redirect trick (clean URLs, hackier), HashRouter (URLs become #/chat, zero-config on GitHub Pages). | HashRouter chosen — right tradeoff for a personal app. Zero configuration required. Clean URLs deferred until custom domain or server-side routing is available. Migrate BEFORE Sprint 2 adds routes. See SW-10. | \*\*Pending (SW-10)\*\* |

| \*\*DEC-036\*\* | Apr 24, 2026 | \*\*Architecture\*\* | \*\*GitHub Actions keep-alive for Supabase free tier\*\* | Supabase free tier pauses the database after 7 days of inactivity. Next visitor hits a dead DB; app breaks until manually resumed in Supabase dashboard. | Upgrade to Supabase Pro ($25/month), Add GH Actions cron ping every 5 days. | GH Actions ping chosen — zero cost, same infrastructure already used for daily email alerts. A single workflow pings both the DB REST endpoint and any Edge Functions to keep both warm. Must be built FIRST in Sprint 3. See SE-8. | \*\*Pending (SE-8)\*\* |

| \*\*DEC-037\*\* | May 13, 2026 | \*\*Architecture\*\* | \*\*Gemini-only for initial AR-5 (Multi-LLM abstraction layer) build; multi-LLM cascade deferred\*\* | AR-5 was designed as a full provider cascade (primary free tier → secondary → stale cache → deterministic). Before building, evaluated whether to implement the full cascade upfront or start with a single provider and add routing later. | Full cascade from day one (Gemini → Groq → cache → deterministic); Gemini-only with abstraction structure preserved; Skip abstraction layer entirely. | Full cascade doubles implementation complexity before validating whether LLM features are even useful. The abstraction layer's value is the single callLLM() entry point — the routing behind it can be added incrementally. Gemini chosen for initial build (generous free tier, no billing required). Groq and additional providers added in a later iteration once the SW-4 (in-app chat panel) proves its value. DEC-025's cascade architecture remains the target end-state. | \*\*Pending\*\* |

| \*\*DEC-038\*\* | May 13, 2026 | \*\*Security\*\* | \*\*Cloudflare Worker proxy required before multi-user launch\*\* | Any API key held by a pure frontend app is visible in network traffic and the JS bundle — there is no technical workaround for this in a browser context. Current personal use (developer is the only user) is safe with BYOK (key stored in localStorage by the developer, in their own browser). This is not acceptable architecture for other users' credentials. | Embed key in codebase (unacceptable — exposed in bundle and git history); BYOK for all users (shifts risk to user, does not protect them); Cloudflare Worker proxy (key stored as Cloudflare secret, never reaches the browser); GitHub Actions as backend (async-only, not viable for real-time chat). | Cloudflare Workers free tier (100k requests/day) is more than sufficient. A ~20-line Worker proxies requests to Gemini; frontend calls the Worker URL, never touches Gemini directly. No user credentials are ever exposed in the browser. AR-11 (Cloudflare Worker proxy for LLM API keys) created to track this work. Trigger: before any non-developer user is given access to the app. | \*\*Pending\*\* |

| \*\*DEC-039\*\* | May 14, 2026 | \*\*Architecture\*\* | \*\*Gemini 2.5 Flash as default model\*\* | Initial implementation used `gemini-2.0-flash`. First live call returned HTTP 404 — Google deprecated `gemini-2.0-flash` for new accounts and instructs callers to use a newer model. | gemini-2.5-flash (current flagship, free tier), gemini-2.0-flash-lite (cheaper, smaller context), gemini-1.5-flash (legacy fallback). | Chose gemini-2.5-flash: current flagship on free tier (1500 RPD, 1M TPD), best reasoning quality at no cost, actively maintained. Model name is a single constant in `src/llm.js` — easy to swap if Google deprecates again. | \*\*Implemented\*\* |

| \*\*DEC-040\*\* | May 14, 2026 | \*\*Architecture\*\* | \*\*Hyperparameter regime tuned for financial advice\*\* | Initial defaults (temperature 0.4, topP unset) were taken from generic chat examples. Financial advice needs higher consistency and tighter focus than creative chat. | Keep generic defaults; OpenAI-style 0.1 temperature; researched Gemini 2.5 best-practice for factual QA. | Temperature 0.4 → 0.2 (research consensus: 0.1–0.4 for factual tasks, 0.1–0.2 ideal for precision-critical domains like healthcare/finance). Added topP 0.75 (tighter nucleus than Gemini's default ~0.95). Left frequencyPenalty/presencePenalty at Gemini default 0.0 — penalties push toward novel tokens which hurts factual content where same terms (SIP, CAGR, dip) must repeat. topK left untouched (Gemini 2.5 has fixed internal cap of 64 regardless of API value). | \*\*Implemented\*\* |

| \*\*DEC-041\*\* | May 14, 2026 | \*\*Signal Logic\*\* | \*\*Signal threshold tuned: asymmetric, more sensitive\*\* | Original symmetric thresholds (Buy Dip ≤-5%, Strong Run ≥+5%) felt conservative — most days every fund sat in Neutral, hiding meaningful moves. | Keep symmetric ±5%; lower symmetrically to ±4%; asymmetric (deeper for dips, shallower for runs). | Default dipPct 5 → 4. Formula updated: Strong Run threshold now uses dipPct/2 instead of dipPct. With dipPct=4 this produces: Buy Dip ≤-4%, Watch -2% to -4%, Neutral ±2%, Strong Run ≥+2%. Asymmetric because runs are confirmed sooner than dips (rises tend to sustain, falls are noisier). Dip threshold buttons updated to [3, 4, 5, 7, 10]. Signal label remains NAV-only; P/E and goal horizon influence conviction at the verdict layer (synthesise function), not the signal label. | \*\*Implemented\*\* |

| \*\*DEC-043\*\* | Jun 02, 2026 | \*\*Architecture\*\* | \*\*Enable Gemini Google Search grounding for chat panel\*\* | In-app chat (SW-4) sometimes returns stale information for "current" questions (Nifty P/E today, recent fund news, market events from the past few weeks). Root cause: Gemini 2.5 Flash has a training cutoff (~early 2025). Our buildContext() already passes live NSE P/E values, but model occasionally ignores context AND we don't pre-fetch every kind of current data. | (a) Pre-fetch more data ourselves (high engineering cost, narrow coverage); (b) Add external search API like Tavily/Brave (extra dependency, may not be free); (c) Enable Gemini's native Google Search tool via `tools: [{ google_search: {} }]` (free tier, no extra dependency, returns citations); (d) Switch to a model with longer training cutoff (doesn't solve "today" queries). | Option (c): Gemini's built-in Google Search grounding. Single-field change in `callLLM()` to opt into Google Search as a tool. Returns citations alongside the response. Live NSE P/E in context remains the primary source — search is a fallback for queries we don't pre-fetch. Tracked as SW-13 (Google Search grounding for chat panel). Note: grounded responses may have higher latency and token usage; monitor via SE-7 (LLM traceability) once implemented. | \*\*Pending\*\* |

| \*\*DEC-042\*\* | Jun 02, 2026 | \*\*Architecture\*\* | \*\*Planner-Narrator pattern: LLMs at the boundaries of agent features, never in the middle\*\* | Agent features (Portfolio Pulse, Goal Compass, Fund Finder, etc.) need both reasoning and computation. Putting an LLM in the middle of financial math is unsafe — hallucinations propagate into recommendations. | LLM does everything end-to-end (unsafe — math errors); deterministic code does everything (no natural language); planner-narrator split (LLM picks tools + narrates results, deterministic code computes). | LLMs sit at the edges: (1) planning — choose which tested function to call, with what inputs; (2) narration — turn structured outputs into plain English. Deterministic JS in goalUtils.js / signal logic does ALL math. Validation layer (SE-5) is itself rule-based, never an LLM. This pattern applies to every agent in AG-1 through AG-5. | \*\*Pending\*\* |

| \*\*DEC-044\*\* | Jun 02, 2026 | \*\*Architecture\*\* | \*\*Manual P/E override as browser-safe fallback for NSE CORS block (SW-7)\*\* | NSE India API (`/api/equity-stockIndices`) is blocked by CORS in all browsers. The three-source cascade originally planned (NSE → Screener.in → Trendlyne) hits the same CORS wall for all providers — there is no free, CORS-friendly source for Nifty/Midcap/Smallcap P/E ratios. | (a) Cloudflare Worker proxy to NSE (AR-11, only needed when sharing app with others); (b) Keep hardcoded fallback updated manually quarterly; (c) Add manual entry UI so user can paste values from NSE website themselves. | Option (c) + (b) combined: hardcoded fallback updated to Jun 2026 values (largecap 21.5, midcap 31.2, smallcap 29.8). New manual P/E override modal (✎ P/E button) lets user enter values from NSE India → Indices page. Stored in `artha_pe_manual` localStorage key. peStatus now has 4 states: live (NSE direct — future), manual (user-entered), fallback (hardcoded), loading. This is the correct pattern until AR-11 (Cloudflare Worker proxy) is built for multi-user use. | \*\*Implemented\*\* |

| \*\*DEC-045\*\* | Jun 05, 2026 | \*\*Architecture\*\* | \*\*Supabase chosen as primary cloud store (AR-1): REST API directly, no SDK\*\* | AR-1 required a cloud database for goals, config, signal history, and decisions. Three options evaluated. | (a) Supabase free tier with @supabase/supabase-js SDK; (b) Supabase REST API directly (no SDK); (c) Firebase Firestore (no RLS, pricing less predictable). | Chose Supabase (open-source, built-in auth + RLS, 500MB free, Postgres) over Firebase. Used PostgREST REST API directly instead of the JS SDK — PostgREST is simple HTTP (GET/POST/PATCH/DELETE with apikey header), adds zero dependencies, and is trivially testable. SDK abstraction adds ~100KB to bundle with no benefit for a personal app. Downside: manual query construction for complex filters (acceptable for this app's query patterns). | \*\*Implemented\*\* |

| \*\*DEC-046\*\* | Jun 05, 2026 | \*\*Security\*\* | \*\*Magic link (OTP) over OAuth for AR-2 auth\*\* | AR-2 required user authentication before cloud data is stored. Two patterns were viable given the existing Supabase dependency. | (a) Supabase magic link (OTP email flow — no password); (b) OAuth via Google/GitHub (adds provider dependencies); (c) No auth (single-user app, all data is personal). | Magic link chosen: zero password management, consistent with Supabase's recommended pattern for personal apps, simpler than OAuth (no redirect_uri config, no provider secrets). Session stored in memory (not localStorage) to avoid token exposure in browser storage. "Continue without account" mode preserved so app works fully without auth. | \*\*Implemented\*\* |

| \*\*DEC-047\*\* | Jun 05, 2026 | \*\*Security\*\* | \*\*RLS policy design: per-user row ownership via auth.uid()\*\* | Every Supabase table row must be restricted to its owner. Two approaches: application-level filtering vs database-level RLS. | (a) Application-level: React app filters by user_id on every query; (b) Database RLS: Postgres enforces user_id = auth.uid() at the DB layer — queries from any client automatically scoped. | Database RLS chosen — defence in depth. Even if the React app has a bug that omits a user_id filter, the DB rejects the query. Service role key (used by GH Actions for writing signal_history) bypasses RLS deliberately — this is the intended pattern for server-side writes. Policy template: SELECT/INSERT/UPDATE/DELETE using (auth.uid() = user_id) with CHECK (auth.uid() = user_id) on INSERT. | \*\*Implemented\*\* |

| \*\*DEC-048\*\* | Jun 05, 2026 | \*\*Architecture\*\* | \*\*Write-through localStorage cache: every Supabase write also updates localStorage\*\* | With Supabase as the primary store, the question is whether localStorage should be maintained in sync or abandoned. | (a) Abandon localStorage after migration (simplest, but breaks offline mode); (b) Read-through cache (read localStorage first, read Supabase on miss — inconsistency risk); (c) Write-through cache (every Supabase write also writes localStorage — both stores always in sync). | Write-through cache chosen. Every `upsertGoal()`, `saveConfig()` call writes to both Supabase and localStorage. If Supabase is unavailable (offline, not configured, not authenticated), the localStorage write still succeeds — the app is never broken. On next online session, Supabase gets the fresh data from the app's live state. No sync logic needed; consistency is maintained by always writing both. Satisfies design principle 5 (USEFUL WITHOUT LLM/SUPABASE). | \*\*Implemented\*\* |
| \*\*DEC-049\*\* | Jun 05, 2026 | \*\*Process / Quality\*\* | \*\*Real test suite mandated; testing rules are non-negotiable (QA-1)\*\* | The project's `.test.js` files had NEVER run — no Vitest installed, no `npm test` script — and `goalUtils.test.js` tested INLINE COPIES of the financial math rather than the real module. "Test coverage" cited in docs was fiction; the financial core had zero regression protection. | (a) Leave as-is (fast but worthless); (b) Convert to a real runner and import real modules; (c) Add a heavyweight e2e stack. | Installed Vitest + jsdom + @testing-library/react; added `npm test`; rewrote every suite to import the REAL modules; added RTL integration tests for UI wiring + regression tests for each fixed bug. Added a ⛔ NON-NEGOTIABLE TESTING RULES block to the top of CLAUDE.md: test real code never copies, tests must actually run, prove teeth by mutation, integration over unit for UI, never report coverage you didn't run. 168 tests passing. | \*\*Implemented\*\* |
| \*\*DEC-050\*\* | Jun 06, 2026 | \*\*Security\*\* | \*\*Auth session in sessionStorage + refresh-token rotation (revises DEC-046)\*\* | DEC-046 kept the session purely in memory, so every page reload forced a fresh magic link — unusable. And access tokens expire (~1h) with no refresh, silently breaking all authenticated writes/reads. | (a) localStorage (persists across browser restarts, largest XSS window); (b) sessionStorage (survives reload, clears on tab close); (c) keep in-memory (most secure, re-login every reload). Plus: refresh on 401. | Chose sessionStorage (balances persistence vs XSS exposure — token not retained across browser restarts) and added `refreshSession()` that exchanges the refresh_token for a new access_token, with `supabaseRequest` retrying once on a 401. Real user_id is decoded from the JWT `sub` claim (the implicit magic-link flow has no user_id URL param). Revises the in-memory decision in DEC-046. | \*\*Implemented\*\* |
| \*\*DEC-051\*\* | Jun 06, 2026 | \*\*Financial Rule\*\* | \*\*Composite goals: per-instrument returns, not one goal CAGR (SW-16)\*\* | A goal funded by a mix of MF SIPs (~12–14%) and RDs/FDs (~7%) has no single "assumed return" — a blended rate that shifts as deposits mature. Entering one CAGR is financially wrong and made the off-track advice nonsensical for deposit-funded goals. | (a) Keep one goal-level CAGR; (b) per-instrument rate with the goal return DERIVED as a contribution-weighted blend, projecting each instrument separately and summing. | Option (b). `projectGoalComposite()` sums MF corpus FV + each MF SIP's FV (own rate) + each RD/FD's value at target (`instrumentValueAtTarget`: known/contracted maturity held flat if matured before target, else accrued to target). `blendedReturn()` is display-only. Off-track levers measure the gap against this composite projection. Backward-compatible: all-MF goals with no per-fund rate project identically to the old single-CAGR model. RD/FD entry is flexible (contribution + rate + dates, with an editable maturity override). | \*\*Implemented\*\* |
| \*\*DEC-052\*\* | Jun 06, 2026 | \*\*Architecture\*\* | \*\*Dynamic fund universe — add/archive, never hard delete (SW-15)\*\* | The fund list was a hardcoded array in App.jsx; the builder churns their real portfolio every 1–3 years and needs to add/retire funds without losing the history of signals/decisions that reference them. | (a) Keep hardcoded; (b) full CRUD with delete; (c) seed + user overlay with archive-only (no hard delete). | Option (c). `src/funds.js` holds `DEFAULT_FUNDS` plus a localStorage overlay `{added, archivedIds}` (`artha_funds_v1`); `App.jsx` derives the effective (non-archived) `FUNDS` from it. Archiving is soft (mirrors the goal-archive model) so historical references stay valid. Managed inline in the goal form. NOTE: the fund overlay is localStorage-only for now; cloud sync deferred to AR-1b. | \*\*Implemented\*\* |
| \*\*DEC-053\*\* | Jun 07, 2026 | \*\*Architecture\*\* | \*\*Generic `user_blobs` key-value table for multi-device cloud durability (AR-1b)\*\* | AR-1 synced the goals table + config blob, but three pieces of state stayed localStorage-only and so were lost on a second device / browser wipe: SW-16 per-fund rates + RD/FD instruments and (after the legacy-edit fix) goalType/startDate/totalYears overrides — all stashed in the `artha_goal_corpus` side-channel — plus the SW-15 fund overlay (`artha_funds_v1`). | (a) Add bespoke columns/tables per blob; (b) widen the goals-table `config_json`; (c) one generic per-user key-value JSONB table keyed by the localStorage key. | Option (c). Migration 004 adds `user_blobs (user_id, key, value JSONB, PRIMARY KEY(user_id,key))` with RLS + the shared updated_at trigger. `supabase.js` exposes `fetchUserBlob(key)`/`saveUserBlob(key,value)` — write-through to localStorage always, cloud upsert (`on_conflict=user_id,key`) when signed in, falling back cleanly when offline. Corpus blob synced from GoalDashboard (debounced 1.5s write-through + ref-guarded read-back; cloud authoritative on login per DEC-048/049, the 1.5s debounce lets the read-back GET win the mount race so a fresh device never clobbers cloud with its local default). Fund overlay synced from App.jsx (immediate write on the deliberate add/archive action + login read-back). Both seeded by `migrateLocalStorageToSupabase()`. Generic over bespoke so future localStorage blobs sync with zero schema work. | \*\*Implemented\*\* |
| \*\*DEC-054\*\* | Jun 07, 2026 | \*\*Financial Rule\*\* | \*\*MF SIPs get a contribution window (start + optional end date), like RD/FD (SW-16b)\*\* | A SIP may have started in the past or be scheduled to STOP before the goal matures (e.g. run the SIP 5 more years for a 10-year goal). The old model assumed every SIP ran from now to the target, which mis-projects deposit-style or time-boxed SIPs. | (a) Keep SIPs running to target always; (b) give each MF SIP a start/optional-end window and project only the active window, then grow the accumulated amount as a lump sum to the target. | Option (b). `goal.funds[fid]` gains optional `startDate`/`endDate`; new `mfSipValueAtTarget()` projects the SIP only over `[max(now,start), min(target,end)]` then grows the resulting lump at the same rate to the target. Past start → contributes from now (past contributions already live in `currentCorpus`, so no double-count); future start → delayed; end before target → stops early; ended in the past → zero future contribution. No dates → identical to the old `futureValueSIP(...)` (backward-compatible — proven by a regression test). Dates round-trip through the form; legacy goals stash them in the corpus side-channel (`fundStartDates`/`fundEndDates`) like `fundRates`. No MF "maturity amount" (market-linked, unknown — captured via corpus + projection). | \*\*Implemented\*\* |



\# How to Use This Document



\- Add a new row immediately when a significant decision is made. Don’t batch updates.



\- When a decision is reversed or superseded, don’t delete the old entry. Change Status to “Reversed” or “Superseded by DEC-XXX”.



\- When starting a new Claude session, upload this alongside the Project Brief and Master Backlog.



\- Signal Logic and Financial Rule decisions deserve extra scrutiny — they directly affect investment recommendations.



\- Security decisions (DEC-026, DEC-027) are non-negotiable design requirements, not optional features.



